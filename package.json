{
  "name": "autopattern",
  "version": "1.0.0",
  "description": "AutoPattern - Browser automation extension with Playwright execution",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "server": "node automation/runners/server.js",
    "install-playwright": "npx playwright install chromium"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "playwright": "^1.40.0"
  }
}
